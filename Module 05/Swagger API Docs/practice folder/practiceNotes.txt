npm install express swagger-ui-express swagger-autogen

project/
 ├─ index.js
 ├─ swagger.js
 └─ routes.js


in index js:
const swaggerUi -> swagger-ui-express
const swaggerFile -> swagger-output.json

now serve + setup
app.use('/docs', swaggerUi.serve, swaggerUi.setup(swaggerFile))

now in app.listen -> generate swagger file before serving.

app.listen(3000, async() => {
    await require(./swagger');
})


in swagger:

const swaggerAutogen

const doc = {

}

const outputFile = 
const routes = 

swaggerAutogen(outputFile, routes, doc);